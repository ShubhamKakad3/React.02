import React, { createContext } from "react"; // 1     these numbers are the steps i took one after another to usnderstand the code writing logic

const UserContext = createContext(); // it is a method for creating a context and stored in userContext    // 2

export default UserContext; // export the context                   // 3

// every context has a provider
